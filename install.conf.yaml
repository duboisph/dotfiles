---
- defaults:
    link:
      create: true
      relink: true

- clean:
  - '~'
  - '~/.config/nvim'
  - '~/.gnupg'

- link:
    ~/.bash_completion.d: bash_completion.d
    ~/.bashrc.d: bashrc.d
    ~/.config/nvim/init.vim: config/nvim/init.vim
    ~/.gnupg/gpg-agent.conf: gnupg/gpg-agent.conf
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.gemrc: gemrc
    ~/.gitconfig: gitconfig
    ~/.gitconfig.local: gitconfig.local
    ~/.gitignore: gitignore
    ~/.hushlogin: hushlogin
    ~/.inputrc: inputrc
    ~/.terraformrc: terraformrc
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf

- shell:
  - command: '[[ -x "$(command -v apt)" ]] && sudo apt update'
    description: Updating APT sources...
  - command: '[[ -x "$(command -v apt)" ]] && sudo apt -y install build-essential git gnupg2 gnupg-agent'
    description: Installing base APT packages...
  - command: 'sh -c "$(curl -fsSL https://raw.githubusercontent.com/Linuxbrew/install/master/install.sh)"'
    description: Installing Homebrew...
  - command: 'brew bundle'
    description: Installing brew formulae...
