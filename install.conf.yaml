---
- defaults:
    link:
      create: true
      relink: true

- clean:
  - '~'
  - '~/.config/nvim'
  - '~/.gnupg'

- link:
    ~/.bash_completion.d: bash_completion.d
    ~/.bashrc.d: bashrc.d
    ~/.config/nvim/init.vim: config/nvim/init.vim
    ~/.gnupg/gpg-agent.conf: gnupg/gpg-agent.conf
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.gemrc: gemrc
    ~/.gitattributes: gitattributes
    ~/.gitconfig: gitconfig
    ~/.gitconfig.local: gitconfig.local
    ~/.gitignore: gitignore
    ~/.hushlogin: hushlogin
    ~/.inputrc: inputrc
    ~/.terraformrc: terraformrc
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf

- shell:
  - command: '[[ -f /etc/lsb_release ]] && sudo apt update'
    description: Updating APT sources...
  - command: '[[ -f /etc/lsb_release ]] && sudo apt -y install build-essential git gnupg2 gnupg-agent'
    description: Installing base APT packages...
  - command: '[[ "$(uname -s)" == "Linux" ]] && sh -c "$(curl -fsSL https://raw.githubusercontent.com/Linuxbrew/install/master/install.sh)"'
    description: Installing Linuxbrew...
  - command: '[[ "$(uname -s)" == "Darwin" ]] && /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"'
    description: Installing Homebrew...
  - command: 'brew bundle'
    description: Installing brew formulae...
